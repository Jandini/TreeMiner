namespace TreeMiner
{
    public enum DepthOption
    {
        /// <summary>
        /// Includes only the current directory in a tree mine operation.
        /// </summary>
        Shallow,

        /// <summary>
        /// Includes the current directory and all its subdirectories in a tree mine operation.
        /// </summary>
        Deep
    }
}
